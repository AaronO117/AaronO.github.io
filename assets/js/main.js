// scripts.js
document.addEventListener("DOMContentLoaded", function() {
    // Example: Update location dynamically
    const locationElement = document.getElementById("location");
    if (locationElement) {
        locationElement.textContent = "cloudy";
    }
});
